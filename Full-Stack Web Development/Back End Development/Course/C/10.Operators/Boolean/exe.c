/*
TODO : C Language Course:-
        ? 10. Operators:
            * Boolean Operators:
                Boolean operators are used to determine the logic between variables or values.
                Operators are used to assign values to variables, add, subtract, multiply, divide and many more.
                Thus:-;
*///* Start Coding......

#include <stdio.h>
#include <stdbool.h>

int main() {
  int x = 10;
  int y = 9;

  bool isGreater = x > y;

  printf("%d", isGreater); 
  
  return 0;
}