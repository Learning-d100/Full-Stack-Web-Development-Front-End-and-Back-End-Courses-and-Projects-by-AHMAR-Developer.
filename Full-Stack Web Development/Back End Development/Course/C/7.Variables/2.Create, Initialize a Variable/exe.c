/*
TODO : C Language Course:-
        ? 7.Variables:
            * 2. Create, Initialize a Variable:-
                You can create a variable by assigning a value to it.
                To create a variable, you must specify the type and give the variable a name.
                You can also assign a value at the same time:
                Syntax:-
                    "type variableName = value;""
                The equal sign = is used to assign a value to the variable.
                So, to create a variable that should store a number, look at the following example:
                Syntax:-
                    "int x = 5;"
                You can also declare a variable first and assign a value later:
                Syntax:-
                    "int x;"
                    "x = 5;"
*///* Start Coding......

// include header file
#include <stdio.h>

// main function
int main() {

    // declare a variable
    int x;

    // assign a value to the variable
    x = 5;

    // declare and assign a vlue to variable at the same time
    int y = 10;

    // use of return statement means that the program will end here
    return 0;

}