/*
TODO : C Language Course:-
        ? 9.Constants:
            * Constants in C
                Constants are values that do not change during the execution of a program.
                They are declared using the const keyword.
                Constants are useful for storing fixed values that should not be modified during the program's execution.
*///* Start Coding......

// include header file
#include <stdio.h>

// main function
int main() {

    // Declare a constant
    const float PI = 3.14159;

    // Print the value of the constant
    printf("The value of PI is: %f\n", PI);

    // use of return statement means that the program will end here
    return 0;

}